import { Container } from "@/components/ui/container";
import { LoginFormCard } from "@/modules/auth/login/login-form-card";

export default function LoginPage() {
  return (
    <Container>
      <LoginFormCard />
    </Container>
  );
}
