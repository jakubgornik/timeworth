{
  "devDependencies": {
    "supabase": "^2.20.12",
    "turbo": "^2.5.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "packageManager": "^npm@10.8.1",
  "dependencies": {
    "@faker-js/faker": "^9.9.0"
  }
}
